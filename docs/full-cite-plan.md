# quarto-full-cite

Planned as a separate Quarto extension providing a `{{< cite @key >}}` shortcode that renders the selected bibliography entry using the bibliography layout of the active CSL style. Native Pandoc citations such as `@key` and `[@key]` remain unchanged.

This file is a temporary implementation note while the standalone extension is developed.
