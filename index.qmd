---
title: "reusable-footnotes"
subtitle: "Page-local reusable bibliographic footnotes for Quarto"
author: "Lucas Sá Barreto Jordão"
date: "2026-09-22"
bibliography: references.bib
---

# Why this pattern matters

`reusable-footnotes` is designed for a citation pattern that is especially common in **Law**. Legal scholarship, briefs, opinions, memoranda, and books frequently place bibliographic references in page footnotes rather than relying only on a final reference list. The same practice is also familiar in **History, Philosophy, Theology, Classics, literary studies, and other Humanities disciplines**.

The important distinction is between the **bibliographic source** and its **footnote occurrences**. BibTeX should define the source once; Quarto should decide how that source is rendered wherever it is used.

For printable outputs, this has an important practical consequence. In **PDF** and **DOCX**, the reader can encounter the full source at the bottom of the same page as the proposition it supports. A reference that is cited again on another page can therefore appear again on that later page, while repeated uses on the *same* page can share one footnote.

The intended page-local behavior is:

```text
page 1     A¹ ... A¹ ... B²
           ─────────────────
           ¹ Full reference A
           ² Full reference B

page 2     A³ ... A³ ... B⁴       # continuous numbering
           ─────────────────
           ³ Full reference A
           ⁴ Full reference B
```

or, when page-local numbering is selected:

```text
page 1     A¹ ... A¹ ... B²
page 2     A¹ ... A¹ ... B²
```

In both cases, the source is repeated **between pages**, but not unnecessarily duplicated **within the same page**.

# BibTeX and full bibliography entries

A normal Pandoc/Quarto citation remains completely unchanged:

```qmd
A normal citation stays normal: [@silva2026].
```

For example: a normal citation stays normal [@silva2026].

The development version of Fred Guth's `bibentry` extension adds a second representation:

```qmd
[@silva2026]{.bibentry}
```

That form prints the **full bibliography entry using the bibliography layout of the active CSL style**. It does not redefine what `@silva2026` means.

Here is the full entry inline:

[@silva2026]{.bibentry}

With:

```yaml
bibliography: references.bib
csl: your-style.csl
```

ordinary citations use the CSL citation layout, while `.bibentry` extracts the corresponding bibliography-layout entry.

# Legal-style footnotes

The useful composition remains native Pandoc syntax:

```qmd
A legal proposition.^[See [@silva2026]{.bibentry}]
```

Rendered here:

A constitutional proposition may be supported in a page footnote.^[See [@silva2026]{.bibentry}]

The Constitution itself can be rendered in full the same way.^[See [@brasil1988]{.bibentry}]

The mechanism is not limited to Law. A historical argument can use the same pattern.^[See [@bloch1953]{.bibentry}]

# Multi-page print demonstration

The following example is intentionally spread over several physical pages using Quarto's native `pagebreak` shortcode. This makes the page-local behavior visible in **PDF** and **DOCX** rather than keeping all examples on one page.

## Demonstration page 1

A printable legal document often develops an argument over several paragraphs while keeping authorities immediately available to the reader. The first occurrence below creates the full footnote for José Afonso da Silva.^[See [@silva2026]{.bibentry}]

A later sentence on the same page can rely on exactly the same bibliographic footnote without printing the long entry a second time.^[See [@silva2026]{.bibentry}]

The Constitution is a distinct source and therefore receives its own footnote.^[See [@brasil1988]{.bibentry}]

This page-local model is useful when the body text is intended to be read on paper or as a paginated digital document: the source remains close to the proposition, but repeated use of the same source does not make the bottom of the page unnecessarily repetitive.

A final sentence on this first page cites the Constitution again. Because the footnote content is identical and the occurrence is on the same page, it reuses the existing note.^[See [@brasil1988]{.bibentry}]

{{< pagebreak >}}

## Demonstration page 2

This second page deliberately cites the same authorities again. The Silva reference must now be printed again at the bottom of this page, because a reader should not need to return to the previous page to identify the source.^[See [@silva2026]{.bibentry}]

A second occurrence on this same page should reuse the new page-local Silva footnote.^[See [@silva2026]{.bibentry}]

The Constitution likewise receives a new page-local occurrence here.^[See [@brasil1988]{.bibentry}]

The point is not to treat the bibliographic source as a new source. BibTeX still contains only one `silva2026` entry and one `brasil1988` entry. What changes is the **placement of the footnote instance** in the printable document.

This page ends by citing the Constitution once more, demonstrating reuse within the second page.^[See [@brasil1988]{.bibentry}]

{{< pagebreak >}}

## Demonstration page 3

A third page repeats the same pattern to make the distinction explicit. José Afonso da Silva appears again as a footnote on this page.^[See [@silva2026]{.bibentry}]

The same source can be invoked again without duplicating the footnote text on this page.^[See [@silva2026]{.bibentry}]

A historical source can participate in exactly the same mechanism, showing that the extension is not specific to legal bibliography.^[See [@bloch1953]{.bibentry}]

A second reference to the historical source on the same page reuses its page-local note.^[See [@bloch1953]{.bibentry}]

The result is a document in which each printed page carries the references needed to understand that page, while identical notes are not redundantly printed twice on the same page.

# Numbering policy

Page-local reuse and footnote numbering are separate choices.

For **continuous numbering** across printed pages:

```yaml
reusable-footnotes:
  scope: page
  numbering: continuous
```

The result is conceptually `1, 2` on the first page, then `3, 4` on the next page, while repeated occurrences on each page reuse the local number.

For **numbering restarted on every page**:

```yaml
reusable-footnotes:
  scope: page
  numbering: page
```

The result is `1, 2` on page 1 and again `1, 2` on page 2.

For PDF/LaTeX, `reusable-footnotes` implements true physical-page awareness using LaTeX's page-tracking mechanism, so naturally flowing page breaks are handled after layout. For DOCX, physical pagination is determined later by Word or another word processor. Exact page-local reuse is therefore deterministic when the document uses explicit Quarto `{{< pagebreak >}}` boundaries; the supplied page-numbering reference document can also make Word restart footnote numbering on every physical page.

# HTML, websites, and books

A standalone HTML document is one scrolling document, so there is no physical-page concept comparable to PDF or DOCX. In that case, the rendered HTML file is treated as one logical page for reusable footnotes.

A Quarto **website or HTML book is different**: each `.qmd` is rendered as its own HTML page. The filter state starts fresh for each rendered page, so every website/book page gets its own canonical footnotes naturally. A source cited on two different HTML pages can therefore appear in both pages' footnote areas, while repeated occurrences within one page share the local footnote.

# Why BibTeX remains the source of truth

The author should not copy and paste formatted references into `^[...]`. The `.bib` entry provides the stable bibliographic identity:

```bibtex
@book{silva2026,
  author    = {Silva, José Afonso da},
  title     = {Curso de direito constitucional positivo},
  edition   = {47},
  address   = {São Paulo},
  publisher = {JusPodivm},
  year      = {2026},
  note      = {Coedição Malheiros}
}
```

The same entry can then be represented in three different ways without duplicating data:

```qmd
Normal citation: [@silva2026]

Full entry: [@silva2026]{.bibentry}

Full entry in a footnote: ^[See [@silva2026]{.bibentry}]
```

# `bibentry` integration

This repository currently vendors an **extended development version** of Fred Guth's [`bibentry`](https://github.com/fredguth/bibentry) extension under `_extensions/bibentry/` for integration testing. The original project is MIT licensed and remains credited to Frederico Guth.

The proposed upstream changes add recursive AST support, including `.bibentry` inside native footnotes; true inline replacement; a single citeproc pass preserving numeric CSL ordering; and portable AST output for HTML, PDF/LaTeX, DOCX, and Typst.

`bibentry` and `reusable-footnotes` have separate responsibilities:

- **`bibentry`** resolves a citation key and renders its full CSL bibliography form;
- **`reusable-footnotes`** decides when an identical footnote occurrence should be reused locally or printed again on a later page.

# Output formats

| Format | Page-local behavior |
|---|---|
| HTML document | the whole HTML file is one logical page |
| HTML website/book | each rendered `.qmd` page has its own footnote scope |
| PDF | true physical-page reuse; identical notes print once per page and may reappear on later pages |
| DOCX | exact reuse within explicit pagebreak-delimited pages; Word can restart numbering on every physical page through the supplied reference document |

No Python runtime or post-render Python step is required.

# References
